{
  "id": "ledger-dev-env",
  "name": "Ledger — Dev (AWS)",
  "values": [
    {
      "key": "base_url",
      "value": "https://<api-id>.execute-api.us-east-1.amazonaws.com/dev",
      "type": "default",
      "enabled": true,
      "description": "URL do API Gateway no ambiente dev (us-east-1). Substitua <api-id> pelo ID real após o deploy. Obtenha com: terraform output api_gateway_url"
    }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "description": "Ambiente dev na AWS (us-east-1). Projeto: double-entry-ledger-dev. DynamoDB table: ledger-dev. Autenticação via API Gateway (configure x-api-key se necessário)."
  }
}

{
  "id": "ledger-local-env",
  "name": "Ledger — Local",
  "values": [
    {
      "key": "base_url",
      "value": "http://localhost:9000/2015-03-31/functions/function/invocations",
      "type": "default",
      "enabled": true,
      "description": "Lambda Runtime Interface Emulator (RIE) local. Suba com: docker run -p 9000:8080 <image>"
    }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "description": "Ambiente local usando Lambda RIE ou SAM Local (sam local start-api). Requer DynamoDB Local rodando na porta 8000 (docker-compose up -d)."
  }
}
